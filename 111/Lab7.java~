import java.util.*;

public class Lab7
{
  public static void main(String [] args)
  {
    Random r = new Random();
    Lab7 lab = new Lab7();
    List someList = new List(20);
    
    for(int i = 1; i <= 20; i++)
      someList.insert(r.nextInt(1000), i);
    
    List revList = lab.reverse(someList);
    
    for(int a = 1; a <= revList.size(); a++)
      System.out.print(revList.getValue(a) + " ");
    
    //System.out.println();
    
    //String test = "[abs[[ end ]]]";
  }
  
  public List reverse (List 1)
  {
    Stack s = new Stack(100);
    
    for(int i = 1; i < 1.size(); i++)
      s.push(1.getValue(i));
    
    List reverseList = new List(1.size());
    
    for(int a = 1; a < 1.size(); a++)
      reverseList.insert(s.pop(), a);
    
    return reverseList;
  }
}